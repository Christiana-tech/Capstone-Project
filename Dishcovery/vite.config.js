import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
});
'/utils/index.js';{
  target: 'https://www.themealdb.com',

  changeOrigin; true,
        rewrite; (path) => path.replace(/^\/utils/index.js/ '')
      }